import type { TripRequest } from "@/types/itinerary";

/**
 * In Phase 2, this will parse the natural language prompt
 * and extract keywords to pre-fill the form state.
 */
export function parsePrompt(prompt: string): Partial<TripRequest> {
  // Stub for now
  return {};
}
